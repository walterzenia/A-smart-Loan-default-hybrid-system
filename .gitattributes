# No LFS - Streamlit Cloud has authentication issues with Git LFS
# Large training files (application_train.csv, bureau.csv, previous_application.csv) 
# are excluded via .gitignore as they're not needed for the deployed app

